# Mock Backend Server for Development

This mock server simulates the backend API for testing purposes.

## Quick Start

1. Install dependencies:
```bash
npm install
```

2. Start the mock server:
```bash
npm run server
```

3. In another terminal, start the frontend:
```bash
npm run dev
```

The mock server will run on http://localhost:8080
